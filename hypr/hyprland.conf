
########################################################################################
AUTOGENERATED HYPR CONFIG.
PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
########################################################################################

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#


# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,1920x1080@144,auto,auto


# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
exec-once = waybar & hyprpaper & fcitx5 & mako 

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# Some default env vars.
env = XCURSOR_SIZE,24
env = LIBVA_DRIVER_NAME,nvidia
env = XDG_SESSION_TYPE,wayland
env = GBM_BACKEND,nvidia-drm
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = WLR_NO_HARDWARE_CURSORS,1

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    touchpad {
        natural_scroll = no
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 7
    border_size = 2
    col.active_border = rgba(f8f8ffee) 45deg
    col.inactive_border = rgba(595959aa)

    layout = dwindle
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 10

   blur {
     enabled = false
     size = 3
     passes = 1
     new_optimizations = on
   }

    drop_shadow = yes
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = off
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device:epic-mouse-v1 {
    sensitivity = -0.5
}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more


# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Q, exec, foot
bind = $mainMod, C, killactive, 
bind = $mainMod, M, exit, 
# bind = $mainMod, E, exec, dolphin
bind = $mainMod, V, togglefloating, 
bind = $mainMod, E, exec, rofi -show drun
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, R, centerwindow,
bind = $mainMod, J, togglesplit, # dwindle
bind = $mainMod, B, focuscurrentorlast,
bind = $mainMod, G, togglegroup,
bind = $mainMod_SHIFT, L, lockgroups, lock
bind = $mainMod, L, lockactivegroup, lock
bind = $mainMod_SHIFT, U, lockgroups, unlock
bind = $mainMod, U, lockactivegroup, unlock
bind = $mainMod, TAB, changegroupactive, forward
bind = $mainMod, O, moveoutofgroup,
bind = $mainMod_ALT, H, moveintogroup, l
bind = $mainMod_ALT, L, moveintogroup, r
bind = $mainMod_ALT, K, moveintogroup, u
bind = $mainMod_ALT, J, moveintogroup, d

# Move focus with mainMod + arrow keys
bind = ALT, left, movefocus, l
bind = ALT, right, movefocus, r
bind = ALT, up, movefocus, u
bind = ALT, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = ALT, F1, workspace, 1
bind = ALT, F2, workspace, 2
bind = ALT, F3, workspace, 3
bind = ALT, F4, workspace, 4
bind = ALT, F5, workspace, 5
bind = ALT, F6, workspace, 6
bind = ALT, F7, workspace, 7
bind = ALT, F8, workspace, 8
bind = ALT, F9, workspace, 9
bind = ALT, F10, workspace, 10
bind = ALT, TAB, workspace, empty

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = SHIFT, F1, movetoworkspace, 1
bind = SHIFT, F2, movetoworkspace, 2
bind = SHIFT, F3, movetoworkspace, 3
bind = SHIFT, F4, movetoworkspace, 4
bind = SHIFT, F5, movetoworkspace, 5
bind = SHIFT, F6, movetoworkspace, 6
bind = SHIFT, F7, movetoworkspace, 7
bind = SHIFT, F8, movetoworkspace, 8
bind = SHIFT, F9, movetoworkspace, 9
bind = SHIFT, F10, movetoworkspace, 10
bind = SHIFT, TAB, movetoworkspace, empty

# Swap active window to a workspace
bind = SHIFT_ALT, right, swapwindow, r
bind = SHIFT_ALT, left, swapwindow, l
bind = SHIFT_ALT, up, swapwindow, u
bind = SHIFT_ALT, down, swapwindow, d

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1
# bind = $mainMod, up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
